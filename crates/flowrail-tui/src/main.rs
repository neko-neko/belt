//! flowrail-tui — human TUI binary (Phase 3)
//!
//! Phase 1 では placeholder。Phase 3 で ratatui-based の rich TUI を実装する。
//! 依存 (ratatui, crossterm 等) は Phase 3 時に crates/flowrail-tui/Cargo.toml に追加。

#![forbid(unsafe_code)]

fn main() {
    eprintln!("flowrail-tui: coming in Phase 3");
    std::process::exit(0);
}
