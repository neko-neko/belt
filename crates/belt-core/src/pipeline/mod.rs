//! `pipeline.yml` model types.
//!
//! See spec §Pipeline Model and §Flag System SSOT for field semantics.
//! The types here are the Rust representation of `pipeline.yml` as written by
//! rule set authors and consumed by the belt runtime.

pub mod model;
