name: status-test
version: 1
args:
  skip_review:
    type: bool
    default: false
phases:
  - id: build
    description: "Build"
    gate:
      - cmd: "true"
  - id: review
    description: "Review"
    when: "!args.skip_review"
  - id: deploy
    description: "Deploy"
