# FB 6-12 Response: SKILL.md Protocol Update + Ticket Actions

## Summary

Address external feedback (FB 6-12) on belt's protocol documentation and backlog.
Primary deliverable: update `skills/belt-agent/SKILL.md` to reflect BELT-24 (regate as independent command) and BELT-29 (status enrichment). Secondary: Linear ticket updates.

## Scope

### In Scope

| FB | Action | Deliverable |
|----|--------|-------------|
| FB 6 | Add `regate` command to SKILL.md, update Decision Rules | SKILL.md update |
| FB 7 | Add step reason → action troubleshooting table | SKILL.md update |
| FB 8 | Document verify-clears-regate in Decision Rules | SKILL.md update |
| FB 10 | Append FB guidance to BELT-28 description | Linear ticket update |
| FB 12 | Create gate cmd timeout ticket | Linear ticket creation |

### Out of Scope (decided during brainstorm)

| FB | Decision | Rationale |
|----|----------|-----------|
| FB 8 (CLI note) | SKILL.md only, no CLI JSON change | belt reports facts; protocol education is skill's responsibility |
| FB 9 | No action | Not needed now |
| FB 11 | No action | "移植が先、制限に当たったら対処" |
| FB 12 (tests) | No action beyond ticket | Test additions accompany feature implementation |

## SKILL.md Redesign

### Target

`skills/belt-agent/SKILL.md` — the runtime skill file LLMs read when driving belt-agent.

### Design Constraints

- Keep total size ~100-110 lines (current: 110 lines)
- Absorb ~20 lines of new content (FB 6/7/8) by removing ~25 lines of waste
- Commands section comment lines (`# Start a new run` etc.) preserved
- HARD-GATE block unchanged
- No CLI behavioral changes

### Waste Removal

| Section | Lines saved | Method |
|---------|-------------|--------|
| Workflow steps 1-6 (散文) | ~7 | Replace with Decision Rules coverage |
| Well-known Config Keys Rules subsection | ~4 | Compress to 1-line footnote |
| Status Output JSON example | ~8 | Compress to key fields only (4 lines) |
| **Total saved** | **~19** | |

### New Content

| Section | Lines added | Source |
|---------|-------------|--------|
| Commands: `regate` entry | +3 | FB 6 |
| Workflow: regate in flow | +1 | FB 6 |
| Decision Rules: regate rows + verify-clears-regate | +4 | FB 6, FB 8 |
| Step Troubleshooting table | +12 | FB 7 |
| **Total added** | **~20** | |

### Final Structure

```
# Belt Protocol
## Commands                        (~20 lines)
  - init, next, verify, regate (NEW), step, status
  - Comment lines preserved
  - `--run <id>` footnote
## Workflow                        (~3 lines)
  - Single ASCII flow including regate
## Decision Rules                  (~12 lines)
  - 5 rows: no-gate, verify-FAIL, regate-targets, no-regate, validate
  - verify-clears-regate noted in regate row (FB 8)
## Step Troubleshooting            (~10 lines)
  - 5 rows: confirmation_required, verify_required,
    regate_not_executed, regate_failed, max_retries_exceeded
## Status Output                   (~8 lines)
  - Compressed JSON example
  - Usage note for context recovery
## Well-known Config Keys          (~6 lines)
  - Table + single-line rules note
## HARD-GATE                       (~10 lines)
  - Unchanged
```

### Full SKILL.md Content

````markdown
---
name: belt-agent
description: Belt Protocol for driving belt-agent CLI. Defines command loop, response interpretation, and safety constraints for LLM agents driving belt pipelines.
user-invocable: false
---

# Belt Protocol

Protocol for LLM agents driving belt-agent CLI — a deterministic state machine for pipeline execution.

## Commands

```bash
# Start a new run
belt-agent init <pipeline.yml>
belt-agent init <pipeline.yml> --arg smoke=true --arg team=BELT

# Get current phase info (or completion signal)
belt-agent next
belt-agent next --run <run_id>

# Run gate checks for current phase
belt-agent verify
belt-agent verify --run <run_id>

# Run regate checks for target phases
belt-agent regate
belt-agent regate --run <run_id>

# Advance to next phase
belt-agent step
belt-agent step --confirm
belt-agent step --run <run_id>

# Inspect full run state (enriched view)
belt-agent status
belt-agent status --run <run_id>
```

`--run <id>` is optional on all commands; omit to use the latest run.

## Workflow

```
init → next → execute → verify (if gates) → regate (if targets) → step → next → ... → completed
```

## Decision Rules

| Situation | Action |
|-----------|--------|
| Phase has no `gate` | Skip `verify`. Go directly to `step`. |
| `verify` returns FAIL | Read `checks` array. Fix failing items. Re-run `verify`. |
| Phase has `regate` targets | After `verify` PASS, run `regate`. On FAIL, fix target phases and re-run `verify` (verify clears regate state). |
| Phase has no `regate` targets | Skip `regate`. Go directly to `step`. |
| Phase has `validate` criteria | Verify each criterion yourself, then `step --confirm`. |

## Step Troubleshooting

When `step` returns `advanced: false`, read the `reason` field:

| `reason` | Action |
|----------|--------|
| `confirmation_required` | Phase has `validate` or `confirm`. Verify criteria, then `step --confirm`. |
| `verify_required` | Run `verify` first. |
| `regate_not_executed` | Run `regate` first. |
| `regate_failed` | Fix regate target phases. Re-run `verify` then `regate`. |
| `max_retries_exceeded` | Escalate. Pipeline author defines recovery via `on_escalation`. |

## Status Output

`status` returns an enriched view assembled from run state, pipeline YAML, and output directories:

```json
{
  "status": "in_progress",
  "current_phase": "review",
  "progress": { "completed": 2, "skipped": 0, "remaining": 2, "total": 4 },
  "phases": [
    { "id": "build", "status": "completed", "verify_passed": true, "attempt": 1, "outputs": ["report.json"] },
    { "id": "review", "status": "current", "verify_passed": false, "attempt": 2, "outputs": [] }
  ]
}
```

Use `status` for context recovery or progress checks.

## Well-known Config Keys

`config` is an opaque map that belt passes through without interpretation.

| Key | Type | Meaning |
|-----|------|---------|
| `config.skill` | `string` | Skill to invoke for this phase. |

Unknown config keys MAY be ignored. Pipeline-specific skills MAY add custom keys freely.

## HARD-GATE

<HARD-GATE>
When validate criteria exist for a phase, you MUST NOT run
`belt-agent step --confirm` without verifying each criterion.

validate is a list of criteria that belt returns for LLM judgment.
belt cannot know whether these criteria were actually evaluated.
The --confirm flag is the LLM's declaration that criteria have been verified.
Passing it without verification is a protocol violation.
</HARD-GATE>
````

## Linear Ticket Actions

### BELT-28: Append FB 10 Guidance

Append to BELT-28 description:

```markdown
## FB Guidance (2026-04-07)

Responsibility split:
- **pause**: CLI responsibility. Save state (`"paused": true` in state.json) and stop.
- **abort**: CLI responsibility. State transition (`current_phase = "FAILED"` sentinel).
- **skip**: Requires design discussion. Skip means "gate failed but proceed anyway" — conflicts with verify-before-step invariant.

Recommended approach for skip:
- Pipeline author declares `on_escalation: skip` on a phase (YAML field)
- LLM/operator runs `step --force-skip` to exercise the permission
- Engine rejects `--force-skip` if `on_escalation: skip` is not declared
- This maintains belt's principle: pipeline author declares, belt enforces

`--force-skip` is NOT a universal escape hatch. It only works when the pipeline author has explicitly authorized skipping for that phase.
```

### New Ticket: Gate cmd Timeout

Create BELT-31 (or next available ID):

```
Title: feat: add timeout to gate cmd execution
Parent: BELT-20
Priority: Medium
Labels: Feature

Description:
gate.rs の execute_cmd() は Command::new("sh").output() を使用しており、
タイムアウトがない。ハングしたコマンド（例: 無限ループするテスト）が
belt-agent 自体を停止させる。

対応案:
- Phase-level timeout field: `gate: [{ cmd: "cargo test", timeout: 120 }]`
- Global default timeout (belt.toml or CLI flag)
- std::process::Command に wait_with_output + thread timeout、
  または wait_timeout crate

注意: async runtime (tokio等) は belt の Non-Goals。同期的な実装が必要。

FB 12 (external review) で指摘。
```

## Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| FB 8: verify-clears-regate | SKILL.md note only, no CLI change | belt reports facts; protocol education is skill's responsibility |
| FB 9: lint --run | No action | Not needed now |
| FB 10: skip mechanism | on_escalation declaration + --force-skip | Pipeline author declares, belt enforces. Universal --force-skip would undermine deterministic guards |
| FB 11: uses nesting | No action | 移植が先、制限に当たったら対処 |
| FB 12: test gaps | Ticket for timeout only | Tests accompany feature implementation |
| SKILL.md size | ~100 lines target | Absorb new content by removing waste, not expanding |
